const baseUrl = 'http://127.0.0.1:8080/v1/';

// Login
export const login = baseUrl.concat('login');
export const loggedUser = baseUrl.concat('user');

// Register
export const register = baseUrl.concat('user');
